struct Name:
  first: str
  last: str

struct User:
  id: int
  name: Name

def main() -> int:
  let mut u: User = User{ id: 0, name: Name{ first: "x", last: "y" } }
  u.name.first := "Ada"
  io.println("first:", u.name.first, "last:", u.name.last)
  return 0
