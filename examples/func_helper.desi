# Function import helper module
# Tests that function imports still work after class import changes

pub def add(a: int, b: int) -> int:
	return a + b

pub def greet(name: str) -> str:
	return "Hello, " + name + "!"
