# EXPECTED: COMPILE_ERROR
# expect: compile_error
# reason: Old match syntax (colon-based), no main function

def tier(x: int) -> str:
	match x:
		case 0:
			return "zero"
		case 1:
			return "small"
		case _:
			return "big"
