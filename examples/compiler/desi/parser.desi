# Minimal Stage-0 stub: return an empty AST JSON compatible with ast.UnmarshalFileJSON.
# Contract: parse_to_json(path: str) -> str

def parse_to_json(path: str) -> str:
  # TODO: replace with real parser that emits full AST JSON
  return "{\"pkg\":null,\"imports\":[],\"decls\":[]}"
