# Test TaskGroup - structured concurrency with implemented syntax

def worker():
    print("Worker running")

def main() -> int:
    print("Testing TaskGroup...")
    
    # Create a task group
    let group = taskgroup_new()
    
    # Check cancellation status
    let is_cancelled = group.is_cancelled()
    print(is_cancelled)  # false
    
    # Wait for all tasks (completes immediately since none spawned)
    group.wait()
    
    # Can also cancel all tasks
    # group.cancel()
    
    print("TaskGroup test complete!")
    return 0
