# Test: List comprehension with range
# This test verifies list comprehensions work with explicit type annotation

def main() -> int:
	let items: list[int] = [x for x in range(0, 3)]
	print("done")
	0
