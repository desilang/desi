def main() -> int:
  let s = "abc"
  let n = str.len(s)
  let mut i = 0
  let mut out = ""
  while i < n:
    let code = str.at(s, i)
    let ch = str.from_code(code)
    out := out + ch
    i := i + 1
  io.println(out)
  return 0
