# Test os module - file I/O functions
import os

def main() -> int:
    # --- Write and Read ---
    let rc = os.write_file("desi_os_test.txt", "hello desi")
    print(rc)
    let content = os.read_file("desi_os_test.txt")
    print(content)

    # --- Append ---
    os.append_file("desi_os_test.txt", " world")
    let content2 = os.read_file("desi_os_test.txt")
    print(content2)

    # --- Exists, is_file, is_dir ---
    print(os.exists("desi_os_test.txt"))
    print(os.is_file("desi_os_test.txt"))
    print(os.is_dir("desi_os_test.txt"))
    print(os.is_dir("."))

    # --- File size ---
    let sz = os.file_size("desi_os_test.txt")
    print(sz)

    # --- mkdir, listdir, rmdir ---
    os.mkdir("desi_os_testdir")
    print(os.is_dir("desi_os_testdir"))
    os.write_file("desi_os_testdir/a.txt", "aaa")
    let files = os.listdir("desi_os_testdir")
    print(files)

    # --- Rename ---
    os.rename("desi_os_testdir/a.txt", "desi_os_testdir/b.txt")
    print(os.exists("desi_os_testdir/a.txt"))
    print(os.exists("desi_os_testdir/b.txt"))

    # --- Remove ---
    os.remove("desi_os_testdir/b.txt")
    print(os.exists("desi_os_testdir/b.txt"))
    os.rmdir("desi_os_testdir")
    print(os.exists("desi_os_testdir"))

    # --- Cleanup ---
    os.remove("desi_os_test.txt")
    print(os.exists("desi_os_test.txt"))

    0

# EXPECTED_OUTPUT:
# 0
# hello desi
# hello desi world
# true
# true
# false
# true
# 16
# true
# ["a.txt"]
# false
# true
# false
# false
# false
