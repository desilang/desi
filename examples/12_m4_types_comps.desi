# EXPECTED: COMPILE_ERROR
# expect: compile_error
# reason: Uses old assignment syntax without let

def main():
	# Missing let keyword
	let a = [x for x in range(10)]
	let s = {"a" for x in range(10)}
