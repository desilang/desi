# Test string concat with Display trait

struct Person:
    name: str
    age: int

impl Display for Person:
    def to_str(self) -> str:
        return "Person"

def main() -> int:
    let p: Person = Person(name="Alice", age=30)
    let greeting: str = "Hello, " + p
    
    return 0
