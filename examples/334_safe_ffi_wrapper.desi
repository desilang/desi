# Safe FFI Wrapper Test
# Tests @extern("C", safe=true, c_name="...") decorator syntax

# Define a safe extern wrapper for abs() - caller doesn't need unsafe block
@extern("C", safe=true, c_name="abs")
pub def absolute(x: int) -> int

def main() -> int:
    # safe=true allows calling without unsafe block!
    let result = absolute(-42)
    print(result)
    0

# EXPECTED_OUTPUT:
# 42
