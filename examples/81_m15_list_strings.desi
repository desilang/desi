# Test: List of strings
def main() -> int:
    let mut names: list<str> = ["alice", "bob", "charlie"]
    names.append("david")
    
    let count: int = len(names)
    
    print(f"Count: {count}")
    
    # Should return 4
    return count
