# EXPECTED: COMPILE_ERROR
# expect: compile_error
# reason: Async functions and old => lambda syntax not fully implemented

async def inc(x: int) -> int:
	x + 1

def main() -> int:
	print("async lambda demo - compile only")
	0
