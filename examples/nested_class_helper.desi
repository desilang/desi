# Nested Class Helper Module
# Tests public and private nested classes

class Container:
	# Public nested class (explicit pub)
	pub class Inner:
		pub mut value: int
		
		pub def get_value(self) -> int:
			return self.value

	# Private nested class (no pub keyword)
	class PrivateInner:
		mut secret: int

	pub mut data: int
