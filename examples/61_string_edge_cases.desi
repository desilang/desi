# String concatenation edge cases  

def main() -> int:
    # Basic string + primitives (each tested separately)
    let msg1: str = "Age: " + 20
    let msg2: str = "Price: $" + 9.99  
    let msg3: str = "Active: " + true
    let msg4: str = "Hello" + " World"
    
    return 0
