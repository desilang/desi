package main
import std.io

def tick(n: i32) -> void:
  io.println("tick=", n)

def main() -> i32:
  defer io.println("bye")   # function-scope defer (runs once at exit)
  let mut i = 0
  while i < 3:
    io.println("i=", i)
    i := i + 1
  return 0
