# This should compile with the C backend now.
def add_i64(a: i64, b: i64) -> i64: a + b
def as_f32(x: f32) -> f32: x
def widen_u16(x: u16) -> u32: x

def main() -> int:
  let _x: i8 = 1
  let _y: u64 = 2
  let _z: f64 = 3.0
  0
