struct User:
  id: int
  name: str

def make_user(i: int, n: str) -> User:
  let mut u: User = User{ id: i, name: n }
  return u

def id_of(u: User) -> int:
  return u.id

def main() -> int:
  let mut a: User = make_user(7, "Ada")
  io.println("id:", id_of(a), "name:", a.name)
  return 0
