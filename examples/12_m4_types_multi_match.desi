# EXPECTED: COMPILE_ERROR
# expect: compile_error
# reason: Uses old => match arm syntax

def main():
	let a: int = 0
	let b: int = 0
	
	match a:
		case 0:
			let x = 1
		case 1:
			let x = 2
		case _:
			let x = 3
