# Library module used by other tests

pub let PUB: int = 1
let PRIV: int = 2

pub struct Point:
  x: int
  y: int

struct Hidden:
  z: int

pub enum Res:
  Ok: int
  Err: none

pub def add(a: int, b: int) -> int:
  return a + b

def secret() -> int:
  return 42
