# Parser-level reserved keyword + checker-level builtin shadow notes.

# This triggers a **parser** error (keyword used as identifier):
struct if:
  x: int

# TIP: If you comment the struct above, the function below would then be type-checked
# and should trigger DTE0021 for shadowing a builtin with parameter name "len".
def f(len: int) -> int:
  return len
