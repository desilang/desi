# EXPECT:
# - DTE0023 "'void' is not a type; use 'none'" (param + return)
# - DTE0023 "'void' is not a type; use 'none'" (let binding type)

def bad(a: void) -> void:     # DTE0023 x2
  return

def main() -> int:
  let x: void = 1             # DTE0023
  return 0
