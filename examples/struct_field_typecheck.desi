struct User:
  id: int
  name: str

def greet(u: User) -> int:
  io.println(u.name)  # should be typed as str
  return 0

def main() -> int:
  # we don't construct a User yet (no struct literals), but type-checking of
  # parameter-based field access is validated via greet(u: User)
  return 0
