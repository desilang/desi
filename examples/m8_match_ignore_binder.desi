enum Result:
  Ok: int
  Err: str

def main() -> int:
  let r: Result = Result.Ok(200)

  match r:
    Ok(_):                   # ignore the payload
      io.println("ok (ignored payload)")
    Err(e):
      io.println("err:", e)

  return 0
