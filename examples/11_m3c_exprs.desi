# EXPECTED: COMPILE_ERROR
# expect: compile_error
# reason: Top-level let not in function, undefined variables

def demo():
    let f = |x: int, y: int| x + y
    let g = |x| x * x
    let evens = [x for x in range(10) if x % 2 == 0]
