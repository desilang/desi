class Point:
    pub x: int
    pub y: int
    
    pub def __new__(px: int, py: int) -> Point:
        # For now, just allocate - field initialization will come later
        let p = Point()
        return p

def main() -> int:
    let p = Point(10, 20)
    return 0
