# Test @classmethod decorator
class Counter:
    total: int
    @classmethod
    pub def get_total() -> int:
        return 0
    @classmethod
    pub def create_batch(n: int) -> list[Counter]:
        return []

def main() -> int:
    let total = Counter.get_total()
    let batch = Counter.create_batch(5)
    return 0
