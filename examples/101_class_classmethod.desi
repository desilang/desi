# Test @classmethod decorator
class Counter:
    pub count: int
    
    @classmethod
    pub def zero() -> Counter:
        let c = Counter()
        c.count = 0
        return c

def main() -> int:
    let c = Counter.zero()
    return 0
