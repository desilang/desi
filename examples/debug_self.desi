# Debug: Check what type 'self' has

struct Person:
    name: str
    age: int

impl Display for Person:
    def to_str(self) -> str:
        # What type is self?
        let x = self
        return "test"

def main() -> int:
    let p = Person(name="Alice", age=30)
    return 0
