# EXPECTED: COMPILE_ERROR
# expect: compile_error
# reason: Async functions not fully implemented

async def add1(x: int) -> int:
    return x + 1

def main() -> int:
    let fut = add1(41)
    let n = await fut
    print("ok")
    0
