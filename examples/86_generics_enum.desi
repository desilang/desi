# Test generic enum with user-defined type (avoiding conflict with built-in Option)

enum MyResult<T>:
    Ok: T
    Empty: none

def main() -> int:
    let x = MyResult.Ok(42)
    
    match x:
        MyResult.Ok(v): print(f"Got: {v}")
        MyResult.Empty: print("empty")

    return 0

