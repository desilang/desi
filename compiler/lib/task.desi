# compiler/lib/task.desi
# Stage-1 placeholder: expose async-friendly signatures.
# These stubs don't implement real async yet; they just type-check.

# Produce a future that completes after ms milliseconds.
pub def sleep_ms(_ms: int) -> future:
  0  # stub future token/id

# Block the current thread until the future completes and return its value.
# (For the demo, just return 0.)
pub def block_on(_fut: future) -> int:
  0
