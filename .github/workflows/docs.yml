name: Docs

on:
  # The book is validated on every branch that could reach a release, and
  # deployed only from main. Before this split, `mkdocs build` first ran as
  # part of `mike deploy --push` — so a broken internal link or a page missing
  # from the nav was discovered by publishing it to the live site.
  push:
    branches: [main, 'dev-**']
    paths:
      - 'book/**'
      - '.github/workflows/docs.yml'
  pull_request:
    branches: [main, 'dev-**']
    paths:
      - 'book/**'
      - '.github/workflows/docs.yml'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to deploy (e.g., 0.1.0)'
        required: true
      alias:
        description: 'Alias (stable, dev, etc.)'
        required: false
        default: 'stable'

permissions:
  contents: write

jobs:
  # Build without deploying, and fail on any warning. --strict is the whole
  # point: mkdocs treats a dangling link, an unresolved reference, or a file
  # absent from the nav as a warning and builds anyway, which on a docs site is
  # indistinguishable from being broken.
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install mkdocs-material mike
          pip install -e book/

      - name: Build the book (strict)
        working-directory: book
        run: mkdocs build --strict

  deploy:
    # Never publish a book that did not build cleanly.
    needs: build
    # Publishing stays exactly where it was: main, a release, or an explicit
    # manual run. The push trigger above now also fires for dev-** branches so
    # they get validated, and the "Deploy dev docs" step below keys off
    # `github.event_name == 'push'` — without this guard, adding dev-** to the
    # trigger would have started publishing the live site from release branches.
    if: >-
      github.event_name == 'release' ||
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'push' && github.ref == 'refs/heads/main')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install mkdocs-material mike
          pip install -e book/

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      # Manual deployment (workflow_dispatch)
      #
      # set-default matters here for the same reason it does on the dev path:
      # mike writes each version into its own directory and the redirect at the
      # root only exists once a default is chosen. Deploying without it leaves
      # the site pointing wherever it pointed before, which looks like the run
      # did nothing at all.
      - name: Deploy manual version
        if: github.event_name == 'workflow_dispatch'
        run: |
          cd book
          mike deploy --push --update-aliases ${{ inputs.version }} ${{ inputs.alias }}
          mike set-default --push ${{ inputs.alias }}

      # Dev deployment on push to main
      #
      # set-default is what writes the redirect at the root of gh-pages. mike
      # publishes each version into its own directory and nothing serves "/"
      # until a default exists, so deploying without it produced a live site
      # that 404'd on its own front page. The release step below sets the
      # default to stable and takes over from this one.
      - name: Deploy dev docs
        if: github.event_name == 'push'
        run: |
          cd book
          mike deploy --push dev
          mike set-default --push dev

      # Release deployment
      - name: Deploy release docs
        if: github.event_name == 'release'
        run: |
          cd book
          VERSION=${GITHUB_REF#refs/tags/v}
          mike deploy --push --update-aliases $VERSION stable
          mike set-default --push stable

      # The documented install command is
      #   curl -sSL https://desilang.org/install.sh | sh
      # so the script has to exist at the site root. mike publishes each
      # version into its own directory and leaves only a redirect at the
      # root, so nothing puts them there by default.
      #
      # Copied from the source commit rather than duplicated into book/docs,
      # so the published installer cannot drift from the committed one.
      # CNAME has to live here for the same reason: GitHub Pages reads it from
      # the root of the published branch, and mike only ever writes version
      # directories plus a redirect. Setting the custom domain in the repo
      # settings creates this file once, but any later `mike deploy` that
      # rewrote the root would drop it and the domain would silently revert.
      - name: Publish installers and CNAME at the site root
        run: |
          set -e
          mkdir -p /tmp/rootfiles
          cp install.sh install.ps1 /tmp/rootfiles/
          echo 'desilang.org' > /tmp/rootfiles/CNAME
          git fetch origin gh-pages
          git checkout gh-pages
          cp /tmp/rootfiles/install.sh /tmp/rootfiles/install.ps1 /tmp/rootfiles/CNAME .
          git add install.sh install.ps1 CNAME
          if git diff --cached --quiet; then
            echo "root files unchanged"
          else
            git commit -m "chore(site): publish installers and CNAME at root"
            git push origin gh-pages
          fi
